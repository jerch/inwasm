{
  "name": "wasm-dummy",
  "version": "0.0.1",
  "description": "wasm/emscripten with typescript and npm bundling test",
  "main": "./lib/index.js",
  "scripts": {
    "test": "mocha lib/*.test.js",
    "tsc": "tsc",
    "watch": "tsc -w",
    "start": "npm run bundle && http-server",
    "prepublish": "npm run build",
    "build-wasm": "bin/install_emscripten.sh && cd wasm && ./build.sh && cd .. && node bin/wrap_wasm.js",
    "bundle": "tsc --project tsconfig.json && node lib/emwasm/cli.js lib/inline.js && webpack",
    "clean": "rm -rf lib dist src/wasm.ts wasm/*.wasm wasm/settings.json",
    "build": "npm run build-wasm && npm run tsc && npm run bundle"
  },
  "repository": "https://github.com/jerch/wasm-dummy",
  "author": "Joerg Breitbart <j.breitbart@netzkolchose.de>",
  "license": "MIT",
  "devDependencies": {
    "@types/mocha": "^9.1.1",
    "@types/node": "^16.11.43",
    "http-server": "^14.1.1",
    "mocha": "^10.0.0",
    "source-map-loader": "^4.0.0",
    "source-map-support": "^0.5.21",
    "typescript": "^4.7.4",
    "webpack": "^5.73.0",
    "webpack-cli": "^4.10.0"
  }
}
